# Laptop-only development dependencies.
# NOT installed at the office (no PyPI there) - keep the office
# requirements.txt free of test tooling. Install on the dev laptop with:
#   pip install -r requirements-dev.txt
-r requirements.txt
pytest>=7.0
